article
	header
	div#admin-messages.post
		- if (notice)
			p =notice

		h1 Messages

		div.messages-list
			ul
				- each message in messages
					li
						h2
							a(href='/admin/messages/' + message._id)!= message.name
						div.meta
							!= moment(message.date).format('LL')
						p
							!= message.body